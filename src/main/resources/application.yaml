server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/scaffold"
    username: root
    password: Pierce Le
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

